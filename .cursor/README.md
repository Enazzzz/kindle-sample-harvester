# Kindle Sample Harvester — Cursor

Project notes and Cursor config for this appliance-style Android app.

## Configure

Add rules, commands, or skills under this `.cursor/` directory as needed, for example:

- `.cursor/rules/` — project conventions
- `.cursor/commands/` — reusable prompts
- Agent instructions can also live in `AGENTS.md` at the repo root
